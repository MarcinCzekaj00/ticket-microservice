spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MarcinCzekaj00/environment-variable-repo.git

server:
  port: ${port:8888}

