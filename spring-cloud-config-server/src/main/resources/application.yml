spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MarcinCzekaj00/ticket-repository/tree/main/environment-variable

server:
  port: ${port:8888}

